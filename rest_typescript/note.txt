golbal: npm install -g typescript ts-node prettier
local: npm install typescript ts-node prettier --save-dev
tsc -v
tsc --init

rest api structuring
npm install yup config cors mongoose pino pino-pretty dayjs bcrypt jsonwebtoken lodash nanoid
npm install --save-dev @types/body-parser @types/config @types/cors @types/express @types/node @types/yup @types/mongoose @types/bcrypt @types/jsonwebtoken @types/lodash
local: npm install typescript ts-node prettier --save-dev
http endpoint<-><middleware calls><->controllers<->services<->databases
run command:
  "scripts": {
    "build": "rm -rf build/ && prettier source/ && tsc",
    "dev": "nodemon --config nodemon.json source/app.ts"
  },

in raw form: node -r ts-node/register source/app.ts